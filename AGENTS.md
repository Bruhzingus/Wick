# Instructions for AI agents and contributors

Before editing this project, read `DESIGN.md`, `ARCHITECTURE.md`, and `CodeBreakdown.md`. Use `CodeBreakdown.md` to locate the smallest relevant set of files instead of broadly scanning the repository.

Treat `DESIGN.md` as the product source of truth and obey every invariant in `ARCHITECTURE.md`. Keep `CodeBreakdown.md` current whenever a change materially alters code ownership, an edit route, a remote contract, a data shape, or an extension recipe.

if the User asks for soemthing that defies the main 2 documents, ask for clarification from the user. 

Utilize asking questionaires from the user when unclear of what direction to take the game.
